name: Java CI with Maven

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  backend-test:
    runs-on: ubuntu-latest

    #    services:
    #      mysql:
    #        image: mysql:8.0
    #        env:
    #          MYSQL_ROOT_PASSWORD: root
    #          MYSQL_DATABASE: projects_management_db
    #        ports:
    #          - 3306:3306
    #        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Install common module
        working-directory: common
        run: mvn clean install

      - name: Build and Run Backend Tests
        working-directory: Projects_Management_BE
        run: mvn -B clean verify
#        env:
#          # Ghi cấu hình DB
#          SPRING_DATASOURCE_URL: jdbc:mysql://localhost:3306/projects_management_db
#          SPRING_DATASOURCE_USERNAME: root
#          SPRING_DATASOURCE_PASSWORD: root

#  frontend-test:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Set up JDK 21
#        uses: actions/setup-java@v4
#        with:
#          java-version: '21'
#          distribution: 'temurin'
#          cache: maven
#
#      - name: Build and Run Frontend Tests
#        # Trỏ vào đúng thư mục Frontend của bạn
#        working-directory: Projects_Management_FE
#        # Lưu ý: Với JavaFX, nếu test có mở cửa sổ (UI), bạn có thể cần cấu hình headless mode
#        # trong code test (ví dụ Monocle) để không bị lỗi trên server không có màn hình.
#        run: mvn -B clean verify